# Main Server for SSC service

## Development

If you don't have `yarn`, please install yarn first. 

1. Install package

```shell
yarn install
```

2. Create .env file & put your MongoDB URL

```shell
cp .env.defaults .env
```

3. Open Server

```shell
yarn server
```